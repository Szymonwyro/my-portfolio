import WorkInProgress from "@/app/components/WorkInProgress";

export default function BackpropPage() {
  return (
    <WorkInProgress
      title="ML Digit Recogniser"
      message="This project page is still under construction."
    />
  );
}